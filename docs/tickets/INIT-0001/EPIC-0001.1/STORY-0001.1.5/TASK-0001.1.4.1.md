# TASK-0001.1.4.1: Create 6 templates with YAML frontmatter

**Parent Story**: [STORY-0001.1.4](README.md)
**Status**: 🔵 Not Started
**Estimated Hours**: 12
**Actual Hours**: -

## Objective

Create 6 default ticket templates (initiative, epic, story, task, bug, generic) with YAML frontmatter field definitions and markdown body scaffolds. These templates establish the foundation for ticket creation and customization.

## Implementation Checklist

### Directory Verification

- [ ] Verify templates directory exists: `test -d skills/gitstory/templates/`
- [ ] If missing, directory should have been created in STORY-0001.1.2

### Template 1: initiative.md

- [ ] Create `skills/gitstory/templates/initiative.md`
- [ ] Add YAML frontmatter with fields:
  - `title` (string, required, 10-100 chars)
  - `timeline` (string, required, e.g., "Q4 2025")
  - `owner` (string, required)
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# INIT-NNNN: [Title]`
  - H2: Objective, Key Results, Epics (table), Success Criteria

### Template 2: epic.md

- [ ] Create `skills/gitstory/templates/epic.md`
- [ ] Add YAML frontmatter with fields:
  - `title` (string, required, 10-100 chars)
  - `parent_initiative` (string, pattern: INIT-NNNN)
  - `story_points` (number, enum: [13, 21, 34, 55])
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# EPIC-NNNN.N: [Title]`
  - H2: Overview, Key Scenarios, Stories (table), Technical Approach

### Template 3: story.md

- [ ] Create `skills/gitstory/templates/story.md`
- [ ] Add YAML frontmatter with fields:
  - `title` (string, required, 10-100 chars)
  - `parent_epic` (string, pattern: EPIC-NNNN.N)
  - `story_points` (number, enum: [1, 2, 3, 5, 8, 13, 21])
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# STORY-NNNN.N.N: [Title]`
  - H2: User Story (As a/I want/So that), Acceptance Criteria, Technical Design, Tasks (table)

### Template 4: task.md

- [ ] Create `skills/gitstory/templates/task.md`
- [ ] Add YAML frontmatter with fields:
  - `title` (string, required, 10-100 chars)
  - `parent_story` (string, pattern: STORY-NNNN.N.N)
  - `estimated_hours` (number, min: 1, max: 8)
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# TASK-NNNN.N.N.N: [Title]`
  - H2: Objective, Implementation Checklist, Files to Create/Modify, Verification

### Template 5: bug.md

- [ ] Create `skills/gitstory/templates/bug.md`
- [ ] Add YAML frontmatter with fields:
  - `title` (string, required, 10-100 chars)
  - `severity` (enum: High, Medium, Low)
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# BUG-NNNN: [Title]`
  - H2: Description, Reproduction Steps, Expected Behavior, Actual Behavior, Environment

### Template 6: generic.md

- [ ] Create `skills/gitstory/templates/generic.md`
- [ ] Add YAML frontmatter with minimal fields:
  - `title` (string, required, 10-100 chars)
  - `description` (string, optional)
  - `status` (enum: Not Started, In Progress, Complete, Blocked)
- [ ] Add markdown body scaffold:
  - H1: `# [Type]-NNNN: [Title]`
  - H2: Description, Details (flexible sections)

### Validation

- [ ] Validate all 6 templates with YAML parser:
  ```bash
  for template in skills/gitstory/templates/*.md; do
    python -c "
  import yaml
  with open('$template') as f:
    content = f.read()
    parts = content.split('---', 2)
    if len(parts) >= 3:
      yaml.safe_load(parts[1])
      print('✓ $template')
    "
  done
  ```

- [ ] Verify each template renders correctly in markdown preview:
  - [ ] Headings display properly (h1, h2, h3)
  - [ ] Lists show bullets
  - [ ] Tables format correctly
  - [ ] YAML frontmatter doesn't render (should be hidden)

- [ ] Verify template structure matches story technical design (lines 40-87):
  - [ ] All required fields present in frontmatter
  - [ ] Field types correct (string/number/enum)
  - [ ] Validation rules defined (required, minLength, maxLength, enum values)
  - [ ] Markdown body follows hierarchy conventions

## Files to Create/Modify

- **CREATE**: `skills/gitstory/templates/initiative.md` (~80 lines, YAML frontmatter + markdown scaffold)
- **CREATE**: `skills/gitstory/templates/epic.md` (~90 lines, YAML frontmatter + markdown scaffold)
- **CREATE**: `skills/gitstory/templates/story.md` (~100 lines, YAML frontmatter + markdown scaffold)
- **CREATE**: `skills/gitstory/templates/task.md` (~80 lines, YAML frontmatter + markdown scaffold)
- **CREATE**: `skills/gitstory/templates/bug.md` (~70 lines, YAML frontmatter + markdown scaffold)
- **CREATE**: `skills/gitstory/templates/generic.md` (~50 lines, minimal YAML frontmatter + flexible scaffold)

## Pattern Reuse

None - This is foundational template creation with no existing patterns to reuse.

## Dependencies

- STORY-0001.1.2 complete: skills/gitstory/templates/ directory exists
- Python 3.12+ with PyYAML for validation

## Notes

**Testing strategy**: Per TESTING.md, templates are markdown files (infrastructure work). Manual validation using bash commands and markdown preview. Python unit tests for template parsing will be created in TASK-0001.1.4.2.

**Field validation**: YAML frontmatter defines field schemas that will be used by planning commands in EPIC-0001.3. The templates themselves are just documentation - validation logic comes later.

**Success criteria**:
- All 6 templates created
- YAML syntax valid (validated with yaml.safe_load())
- Markdown renders correctly in preview
- Field definitions match story technical design
