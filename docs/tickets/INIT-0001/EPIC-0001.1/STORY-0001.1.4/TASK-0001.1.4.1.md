# TASK-0001.1.4.1: Research anthropics/skills and create config.json

**Parent Story**: [STORY-0001.1.4](README.md)
**Status**: 🔵 Not Started
**Estimated Hours**: 2
**Actual Hours**: -

## Objective

Research SKILL.md structure patterns from anthropics/skills repository and create valid .claude-plugin/config.json for marketplace registration. This establishes the foundation for SKILL.md creation in TASK-0001.1.4.2.

## Implementation Checklist

### Skills Repository Research

- [ ] Browse https://github.com/anthropics/skills repository
- [ ] Read repository README for conventions and best practices
- [ ] Review 3-5 representative skills as examples (aim for 3, review up to 5 if patterns unclear)
- [ ] Document findings inline below for each skill reviewed:
  - [ ] Skill name: _______________
  - [ ] Heading structure: (e.g., "h1 > h2, 5 sections")
  - [ ] Activation section: (format and placement)
  - [ ] Word count: (approximate range)
  - [ ] Code block patterns: (bash/python/other)
  - [ ] YAML frontmatter: (YES/NO)
- [ ] Summarize common patterns (document in checklist above after reviewing 3+ skills):
  - [ ] Typical heading hierarchy: _______________
  - [ ] Activation section placement: _______________
  - [ ] Word count range observed: _______________
  - [ ] Best practices identified: _______________
- [ ] If anthropics/skills unavailable: Use Claude Desktop Community docs as fallback

### Create .claude-plugin/config.json

- [ ] Create `.claude-plugin/` directory if needed: `mkdir -p .claude-plugin`
- [ ] Create `.claude-plugin/config.json` with 9 required fields:
  - [ ] `name`: "gitstory"
  - [ ] `id`: "gitstory-ai/gitstory"
  - [ ] `version`: "0.1.0"
  - [ ] `entry_point`: "skills/gitstory/SKILL.md"
  - [ ] `author`: "Bram Swenson"
  - [ ] `description`: "Workflow-agnostic ticket management via plugin-based state machines"
  - [ ] `keywords`: Array with 8 items (workflow, tickets, state-machine, planning, BDD, kanban, scrum, agile)
  - [ ] `license`: "MIT"
  - [ ] `repository`: "https://github.com/gitstory-ai/gitstory"

### Validation

- [ ] Validate JSON syntax:
  ```bash
  python -m json.tool .claude-plugin/config.json
  ```
- [ ] Verify all 9 required fields present
- [ ] Verify keywords array has 8 items
- [ ] Verify entry_point path is correct (relative from repo root)
- [ ] Note: Entry point file doesn't exist yet (created in TASK-0001.1.4.2)

### Optional: Python Unit Test

- [ ] (Optional) Create `tests/test_skill_config.py` for CI validation if time permits within 2-hour estimate

Note: Python tests are optional and can be deferred to EPIC-0001.2. Primary validation uses bash commands above.

## Files to Create/Modify

- **CREATE**: `.claude-plugin/` (directory)
- **CREATE**: `.claude-plugin/config.json` (~15 lines, JSON with 9 required fields)
- **CREATE** (optional): `tests/test_skill_config.py` (~50 lines, Python unit tests)

## Pattern Reuse

None - This is research and configuration work with no existing patterns to reuse.

## Dependencies

- STORY-0001.1.2 complete: Verify with `test -d skills/gitstory && test -f skills/gitstory/README.md && grep -q '{baseDir}' skills/gitstory/README.md`
- Browser/git access to https://github.com/anthropics/anthropic-quickstarts
- Python 3.12+ for validation

## Notes

**Testing strategy**: Per TESTING.md, this is configuration work. Use manual validation with bash commands (required). Python unit tests are optional - add only if time permits within 2-hour estimate, otherwise defer to EPIC-0001.2.

**Research scope**: Review at least 3 skills, document common patterns thoroughly to inform SKILL.md creation in next task.

**Entry point path**: Use `skills/gitstory/SKILL.md` (relative from repo root). File will be created in TASK-0001.1.4.2.
